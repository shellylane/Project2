<script>
    $(function () {
        // page is now ready, initialize the calendar...
        $('#calendar').fullCalendar({
            editable: true,
            header: { center: 'month,listMonth' },
            // buttons for switching between views
            views: {
                month: { // name of view
                    titleFormat: 'MMMM YYYY'
                    // other view-specific options here
                },
                listMonth: {
                }
            },
             
            eventSources: [
                // your event source
                {
                    url: '/api/events', // use the `url` property
                    color: 'seagreen',    // an option!
                    textColor: 'black'  // an option!
                }
                // any other sources...
            ]
        });
    });
</script>

<div class="container">
    <div class="row">
        <div class="col">
            <h1>Schedule</h1>
            
        </div>

    </div>
    <div class="row">
        <div class="col"></div>
        <div id='calendar' class="col-8"></div>
        <div class="col"></div>

    </div>

    
</div>